The user wants a rigorous PRD for AuraOS Component #07: Reference Boot, Firmware Handoff, Serial Console & Clocks. This is a freestanding C OS project supporting 4 architectures. Let me think about what 4 architectures make sense: x86_64, aarch64, riscv64, and... loongarch64? Or maybe i686? Given "4 arches" — likely x86_64, aarch64, riscv64, loongarch64 is a common quartet for modern OSDev. Or x86_64, aarch64, riscv64, i686. I'll go with x86_64, aarch64, riscv64, loongarch64 — but honestly i686 might be safer/more common. Let me just pick: x86_64, aarch64, riscv64, loongarch64. Actually, for a "reference boot, firmware handoff" component, QEMU targets are typical: pc/q35 (x86_64), virt (aarch64), virt (riscv64), virt (loongarch64). QEMU supports loongarch64 virt since 8.x. I'll define the 4 arches explicitly at the top so there's no ambiguity: x86_64, aarch64, riscv64, loongarch64.

The PRD must follow the exact 6-section structure. It must be production-grade markdown, grounded in "Clean. Simple. Small. Fast. Direct. No overengineering." So the PRD itself should embody minimalism: small bounded interfaces, fixed-size structures, no dynamic allocation, no printf formatting bloat (maybe a tiny format subset), polling UART only, monotonic clock via single counter read.

Key content:

1. Executive Summary & Scope Boundary
- AuraOS component #07 — the earliest code that runs after firmware. Three responsibilities: (a) reference boot flow (Multiboot2 on x86_64, device-tree-based handoff for aarch64/riscv64/loongarch64), (b) parse firmware-provided boot structures into a single normalized `aura_bootinfo` block, (c) poll-mode serial console output + input, (d) monotonic timekeeping (uptime counter) with no IRQ dependency.
- Position in system: between firmware (BIOS/UEFI via GRUB/limine? or direct?) — keep it simple: Multiboot2 via GRUB for x86_64; for the others, raw kernel entry from QEMU -kernel with device tree. Provide handoff into kernel main with a single pointer to bootinfo.
- Scope boundary table: in scope / out of scope.

Actually, wait — should I use Limine? Multiboot2 with GRUB is the classic and simplest and covers x86_64 and even aarch64 (GRUB has arm64-efi). But keeping it "reference boot" — the simplest robust path:
- x86_64: GRUB2 Multiboot2, 32-bit entry, build long mode page tables, jump to 64-bit kernel. That's the classic reference path.
- aarch64/riscv64/loongarch64: QEMU `-kernel` loads ELF64 at fixed address, starts at ELF entry in EL1/S-mode/PLV0; firmware handoff = device tree blob pointer in a register (x0 on aarch64, a1 on riscv64; loongarch passes... loongarch virt passes ACPI? Actually LoongArch virt passes FDT-like? LoongArch uses ACPI Madt... hmm. QEMU loongarch64 virt passes a "struct loongarch_boot_info"? Actually QEMU loongarch64 virt uses ACPI and passes boot info via fw_cfg? Let me recall: QEMU hw/loongarch/virt.c passes `a0 = boot_info address`? For LoongArch, the kernel is loaded and boot params passed via ACPI + a0 points to a struct. Linux on LoongArch uses EFI stub or the "boot params" passed in a0 pointing to `struct boot_params` from QEMU. Hmm, this could get deep. To keep PRD honest but not over-specify wrong details, I can say: loongarch64 handoff per QEMU `virt` machine: entry at ELF header, `a0` = pointer to firmware-provided boot parameter block; component parses memory map from the QEMU-provided structure and ACPI-independently. I can define parsing as "memory map via fw_cfg DMA interface" — actually fw_cfg is the clean cross-arch escape hatch: QEMU fw_cfg provides E820/NUMA info on all four virt machines. But adding fw_cfg driver adds size. Hmm, but for aarch64/riscv64 FDT memory node is easiest. For loongarch, the QEMU-provided boot params include memory map.

Alternative: choose arches x86_64, aarch64, riscv64, i686 — all with well-known simple boot paths. But "4 arches" for a modern OS is more likely all-64-bit. I'll go x86_64, aarch64, riscv64, loongarch64 and be careful with loongarch details: QEMU loongarch virt passes a pointer in a0 to `struct loongarch_boot_info`? Let me recall the actual QEMU code: hw/loongarch/virt.c builds `struct loongarch_boot_info` in RAM (with fw_cfg, memory map, a "linux,uefi" stuff) and sets a0 = boot_info_addr? Actually for direct kernel load, QEMU loongarch virt passes: a0 = address of boot_params (struct boot_params from Linux arch/loongarch/kernel/head.S era — `struct boot_params { u64 efi_fw_virt; ... u64 efi_memmap...; u64 memstart; u64 memsize?; struct efi_mmap ...; u64 cmdline; u64 extlist; u64 env; }`). There's a known `struct loongarch_boot_params` in QEMU. To avoid over-specifying possibly-wrong fields, the PRD can mandate: "loongarch64: QEMU `virt` machine, `-kernel` direct load; firmware handoff is the boot-params block pointer passed in `a0` at entry; the parser reads the UEFI-style memory descriptor array embedded therein (firmware-provided, QEMU-generated), vendor GUIDs ignored, only type/phys/npages consumed." That's defensible and grounded. Alternatively use fw_cfg file "etc/e820" via fw_cfg DMA on loongarch — QEMU does expose e820 via fw_cfg on some machines. Keep it: parse boot params memory map.

Honestly, the PRD doesn't need to be an implementation guide with QEMU source citations; it needs concrete contracts. I can specify per-arch handoff registers and the normalized bootinfo ABI, with the parsers as requirement statements. I'll keep loongarch at: a0 → QEMU boot-params block; memory descriptors UEFI-format; UART at 0x1fe001e0 (Loongson 3A5000 UART0 is at 0x1fe001e0, 16550-compatible,.shift=0? The Loongson UART is 16550 with 8-bit reg stride at 0x1fe001e0 with frequency 100MHz? Actually LS7A UART clock... The loongarch virt machine UART base 0x1fe001e0, reg-shift=0, reg-io-width=1? QEMU hw/loongarch/virt.c: uart at VIRT_UART_BASE = 0x1fe001e0, with `sysbus_create_simple("serial", mmio, qemu_allocate_irq...)` — 8250 with 1-byte stride, 100MHz input clock? The serial device in QEMU uses clock of... it's set via serial_mm_init with... it's fine: PRD will say base 0x1fe001e0, stride 1, input clock 100 MHz, divisor programmed by firmware/boot? Hmm — for a raw poll-mode UART we still need a working divisor. On x86 COM1 the BIOS/GRUB has already programmed the divisor. On QEMU virt machines, the serial device ignores baud in practice? No — QEMU's serial model does emulate baud rate loosely: it uses it to throttle only if `guest_errors`/it actually checks baud? QEMU serial does compute baud and uses it for character pacing (chardev write happens regardless; QEMU doesn't throttle on baud for output to stdio — actually it can throttle via `fifo` and `baud`... in practice output appears instantly regardless of divisor). For correctness we program divisor from a fixed table; on FDT arches we read `clock-frequency` from the node; on loongarch fixed 100MHz? Actually Loongson LS7A UART clock is 100 MHz? The 7A1000 UART uses 100MHz base? Hmm. Known values: ls7a uart clock 100MHz? Linux `drivers/tty/serial/8250/8250_lpc.c`? For Loongson-3 with LS7A, UART clock is 100 MHz? I recall `#define DEV_LPC_CLK 100000000`? There's `loongson,ls7a-uart` compat with clock 100MHz in Linux dts (loongson64-2k1000.dtsi uses clock-frequency = <100000000>). Yes — Loongson UARTs commonly 100MHz. QEMU loongarch virt dts? QEMU loongarch virt doesn't provide FDT by default (it provides ACPI + boot params). Fine: fixed 100 MHz constant for the reference board, baud 115200 → divisor = 100e6/(16*115200) = 54.25 → 54. Good.

For x86_64: COM1 0x3F8, clock assumed 115200-capable; standard is 1.8432MHz → divisor 1 for 115200. GRUB leaves UART configured; we re-init to divisor 1.

aarch64 virt: PL011 at 0x09000000, clock 24MHz (PL011 on virt: clock 24MHz, no IBRD/FBRD needed for poll-mode TX since it just writes to FIFO; baud regs irrelevant for TX in QEMU but program anyway IBRD=13, FBRD=1 for 115200 @ 24MHz). riscv64 virt: NS16550A at 0x10000000, clock 3.6864MHz? QEMU riscv virt UART: `serial_mm_init(..., 3.6864MHz?...)`. Actually QEMU hw/riscv/virt.c: serial_mm_init(system_memory, memmap[VIRT_UART].base, 2, ...clock?) — it uses `serial_hd(0)`, regshift 2? Hmm: riscv virt ns16550a at 0x10000000 with reg-shift = <2>? In QEMU riscv virt dts: `reg-shift = <2>`? Hmm, actually I recall riscv virt uart has `reg-shift = <2>`? Hmm... The QEMU riscv `virt` machine creates the UART with `serial_mm_init(address_space_mem, memmap[VIRT_UART].base, 2, ...)` where 2 is itmm? The signature: serial_mm_init(MemoryRegion*, hwaddr base, unsigned it_shift, qemu_irq, int baudbase, Chardev*, enum device_endian) — for riscv it_shift=2? For virt.c I believe reg-shift