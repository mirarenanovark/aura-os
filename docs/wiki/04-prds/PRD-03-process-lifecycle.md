The user wants a rigorous PRD for AuraOS Component #03: Process Lifecycle, Scheduler, Timers & Native ELF ABI. It's a freestanding C OS, 4 architectures (presumably x86_64, aarch64, riscv64, and one more — maybe i686 or loongarch64; I'll pick x86_64, aarch64, riscv64, and... a common 4th would be i686 (x86 32-bit) or loongarch64. Let me think — for a hobby/production hybrid OS with 4 arches, x86_64, aarch64, riscv64, i686 is plausible. Or loongarch64. I'll go with x86_64, aarch64, riscv64, i686 — that keeps the story simple (2 x86 + 2 RISC families). Actually riscv64 + aarch64 + x86_64 + i686 makes sense. I'll state that explicitly in scope.

Key design principles the user demands: Clean. Simple. Small. Fast. Direct. No overengineering. So the PRD must reflect a minimal, ruthless design: e.g., a single flat scheduler (no MLFQ/CFS), fixed priority + round-robin within priority, O(1) runqueues, bitmap scan. Timers: monotonic nanosecond timebase, a simple timing wheel or sorted absolute deadline list (K亿... keep it simple: a hashed timing wheel with 64 buckets or a simple sorted doubly-linked list — for no overengineering, a per-CPU sorted list with insertion is O(n); better: timing wheel. But "no overengineering" — a simple 64-bucket timing wheel is fine and standard. I'll pick a per-CPU timing wheel, 256 buckets, NS_PER_TICK 1ms). Actually let me think about what's genuinely defensible:

Scheduler: priority-based round-robin, 32 priorities (NUM_PRIORITIES 32), bitmap + runqueue arrays per CPU, O(1) pick via CLZ/CTZ. Timeslice 10ms default. No CPU affinity pinning beyond "current CPU unless migrated by idle balance" — maybe simple: tasks run on the CPU they were woken on; no load balancer in v1 except idle stealing. Anti-goal: no CFS, no NUMA awareness, no user-defined affinities.

Process lifecycle: fork/exec/wait/exit, zombie reaping, PID allocation with bitmap, process table fixed size (e.g., 4096 processes, 8192 threads? keep bounds concrete). Or dynamic? For "small" — fixed-size static tables are defensible for a micro-kernel-ish freestanding OS. I'll specify PROC_MAX = 1024 processes, THREAD_MAX = 4096 threads, PID/`tid` from bitmap allocator with wraparound guard.

Actually — do we have threads? A component named "Process Lifecycle, Scheduler, Timers & Native ELF ABI" — threads are typically part of process lifecycle. Keep it simple: processes have a single main thread in v1; kernel threads exist. Multi-threaded userspace is an anti-goal for #03 (deferred to a later component). Hmm, but that could be seen as underdelivering. Let me think: The user's style (based on the "Anti-Goals" section requirement) suggests they like ruthless scope cutting. I'll include kernel-side task structures that are thread-capable (task = schedulable entity), but restrict v1 to one user thread per process; pthread-like support is explicitly out of scope. That's honest and matches "no bloat."

ELF ABI: freestanding static ELF64 executables. Support ET_EXEC and maybe ET_DYN with fixed base? Keep it simple: static ET_EXEC only, plus optional static-pie? No — anti-goal: no dynamic linking, no shared libraries, no ELF relocations beyond R_*_RELATIVE? Actually a static ELF with relocations would need reloc processing. Simplest: require ET_EXEC with PT_LOAD segments, p_paddr/vaddr identity or arch-specific base (e.g., 0x400000 on x86_64, 0x400000 on i686, 0x400000 on aarch64, default 0x10000? on riscv64 typical is 0x10000 or 0x400000). Keep: required PT_LOADs, one PT_STACK? Or fixed stack allocated by kernel. Auxv: AT_PAGESZ, AT_PHDR..., AT_ENTRY, AT_RANDOM, AT_SECURE=0, AT_NULL. No AT_SYSINFO_EHDR vdso in v1 (anti-goal? or include AT_SYSINFO_EHDR — that's for vDSO fast gettimeofday; anti-goal: no vDSO in v1; time via syscall). Entry ABI: stack layout argc/argv/envp/auxv per SysV.

Wire ABI contracts: syscall numbers shared across arches (a unified syscall table, args in registers per-arch: x86_64 rdi/rsi/rdx/r10/r8/r9, nr in rax, return rax, -errno in range -4095..-1; aarch64 x0-x5, x8; riscv64 a0-a5, a7; i686: use int 0x80 with ebx/ecx/edx/esi/edi/ebp). That's concrete and good.

Syscall list for this component: 
- task: fork? Actually maybe `spawn` (fork+exec combined) to keep simple? Classic: clone/fork/exec/exit/waitpid. AuraOS minimal set: `proc_fork() -> pid`, `proc_exec(path, argv, envp)`, `proc_exit(status)`, `proc_waitpid(pid, *status, flags)`, `proc_getpid()`, `proc_sleep_ns(ns)` (nanosleep), `time_now_ns()`, `timer_arm`? Keep timers: `time_monotonic_ns()`, `time_deadline...` Hmm. Timers component: monotonic clock read, nanosleep, and kernel-side timer callbacks (ktimer) for kernel use. User timers (interval timers, POSIX timer_create) are anti-goals.

Also scheduling syscalls: `proc_yield()`, maybe `proc_nice(delta)` — keep: `sched_yield()`, `sched_nice`? Minimal: yield only; priorities fixed by policy internally. Anti-goal: no user-controllable priorities. Hmm — maybe allow setpriority limited range? Anti-goal keeps it out. I'll include `sched_yield()` and `sched_getpid/tid`. 

Kernel internal C interfaces (not syscall): struct task, struct cpu_ctx/runq, timer API: `timer_call_at(deadline_ns, fn, arg)` one-shot kernel timers; cancellation via timer_cancel(handle) returning bool. DPC-like softirq? Keep simple: timers execute in interrupt context at tick, must not sleep; document that.

Data structures with C code:
- struct task { ... } — fields: list node, cpu idx, state, prio, pid, ppid, exit status, kstack, mm ref, timeslice, deadline for sleep, name[16].
- runqueues: struct runq { bitmap; struct list heads[NPRIO]; count; }
- timing wheel: struct timer_wheel { bucket[256] lists; u64 next_expire_ns; u64 last_tick_ns; } plus `struct ktimer { node; deadline_ns; fn; arg; flags; }`.
- pid allocator: bitmap + last hint.
- wait queue? Keep it as a simple `wq` with list + wake. Maybe too much. Include minimal `struct waitq` for sleep/wake used by nanosleep and waitpid? Actually nanosleep can use a ktimer that wakes the task. waitpid uses a child-exit list check + sleep loop. Simple.

ELF loader: `elf_load(mm, buf, len, *entry)` checks EI_MAG, ELFCLASS64 (ELFCLASS32 on i686), e_machine per arch, ET_EXEC only, PT_LOAD mapping with page alignment, rejects W+X segments, zero-fill bss, requires that segments don't overlap kernel range. Also stack setup function `arch_setup_user_stack(...)` producing auxv. Provide auxv enum and stack layout diagram.

Failure modes & limits: errno values (ENOMEM, EAGAIN, EINVAL, EFAULT, ENOENT, E2BIG, ESRCH, ECHILD, ETIMEDOUT, EINTR?), bounds: argv total 32 KiB? environ 32 KiB? ARG_MAX 128 KiB total (args+env). Stack 256 KiB default guard + grow? No growth: fixed 256 KiB user stack with guard page. Max processes 1024, kernel threads 64, timers per CPU 256, timer callback budget per tick (e.g., fire at most 64 callbacks per tick, defer rest), PID space 32768 with wrap reuse after full pass (or PID_MAX 65536 no reuse until table free). Keep concrete numbers.

Acceptance criteria: measurable: syscall latency targets (yield < 1µs typical on x86_64 QEMU... maybe avoid hard perf claims; use "fork+exec+exit+wait < 250µs median on QEMU x86_64 -accel tcg"? TCG is slow. Use KVM-required perf gates or mark perf targets as advisory on emulator, hard on hardware. I'll write: gates measured on QEMU TCG with generous bounds or on reference hardware. Provide test gates: TL-sched-001 round-robin fairness within bounds, starvation-free at prio inversion? Priority scheduling means lower prio starves by design — document that as intended (SCHED policy: strict priority with round robin within level; interactive niceness out of scope). Timekeeping: clock monotonic never regresses per CPU; cross-CPU skew bounded (< 1ms after sync). Timer coalescing accuracy ±1 tick. ELF loader rejects malformed binaries (fuzz corpus), stack layout validated by a userspace test program reading auxv.

Also "Wire ABI contracts" — ABI stability: syscall numbers ABI frozen, struct layouts frozen where shared with userspace (e.g., `struct sys_procinfo` for a future `proc_info` syscall? Might be bloat. But "wire ABI" also covers ELF auxv, errno values, syscall ABI per arch. Include a small `wire` table: syscall IDs enum with numbers.

Deliverables/evidence: test binaries under tests/, CI runner, coverage of failure paths via fault injection (OOM injection at Nth allocation).

Non-functional: code size budget (e.g., scheduler+timers+proc+elf ≤ 4500 SLOC excluding arch asm; .text footprint ≤ 64 KiB per arch? Keep: ≤ 6000 SLOC total, ≤ 120 KiB .text+rodata combined), worst-case interrupt-to-schedule latency, deterministic: no unbounded loops, all loops bounded by constants, MISRA-ish constraints: no dynamic allocation in scheduler path, no recursion, IRQ-safe locking discipline documented.

Also 4 arch handling: per-arch context switch (x86_64 fxsave/xsave? Simple: use xsave if available else fxsave — that might be complexity. For v1: save integer regs + callee-saved FP via fxsave region fixed 512 bytes; SSE required on x86_64 anyway. aarch64: save x0-x30, sp, pc, pstate, fpu regs q0-q31 + fpcr/fpsr (or lazy? no — eager, simple). riscv64: integer regs + fpu if enabled (off in v1 → FPU disabled in user? That breaks floating point in userspace. Must enable FPU and save it. Simplest: eager save of